apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pythontest-hpa
  namespace: pythontest
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pythontest
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
